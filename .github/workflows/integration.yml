name: Integration

on:
  pull_request:
    branches:
      - main

permissions:
  pull-requests: write
  actions: read
  contents: read

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Run tests with coverage
        run: mise run test:coverage
        timeout-minutes: 8

      - name: Generate coverage report
        if: hashFiles('coverage.out') != ''
        run: |
          go tool cover -html=coverage.out -o coverage.html

      - name: Upload coverage artifact
        if: hashFiles('coverage.out') != ''
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ github.event.pull_request.head.sha }}
          path: coverage.*

      - name: Calculate PR coverage
        id: coverage
        if: hashFiles('coverage.out') != ''
        run: |
          coverage=$(go tool cover -func coverage.out | grep total | awk '{print $3}' | sed 's/%//')
          echo "current_coverage=$coverage" >> $GITHUB_OUTPUT
          echo "Coverage: ${coverage}%"

      - name: Get main branch coverage
        id: main-coverage
        run: |
          MAIN_SHA=$(git rev-parse origin/main)

          # Try to download cached coverage artifact from main
          if gh run download -n "coverage-$MAIN_SHA" --repo ${{ github.repository }} --dir /tmp/main-coverage 2>/dev/null; then
            main_coverage=$(go tool cover -func /tmp/main-coverage/coverage.out | grep total | awk '{print $3}' | sed 's/%//')
            echo "main_coverage=$main_coverage" >> $GITHUB_OUTPUT
            echo "Main coverage (cached): ${main_coverage}%"
          else
            # No cached artifact — check out main and run coverage
            cp coverage.out /tmp/pr-coverage.out 2>/dev/null || true
            git checkout origin/main -- .
            mise run test:coverage || true
            main_coverage=$(go tool cover -func coverage.out | grep total | awk '{print $3}' | sed 's/%//')
            echo "main_coverage=$main_coverage" >> $GITHUB_OUTPUT
            echo "Main coverage (computed): ${main_coverage}%"

            # Upload for future PRs
            cp /tmp/pr-coverage.out coverage.out 2>/dev/null || true
          fi

      - name: Generate coverage comparison report
        id: coverage-report
        run: |
          current_coverage="${{ steps.coverage.outputs.current_coverage }}"
          main_coverage="${{ steps.main-coverage.outputs.main_coverage }}"

          current_coverage=${current_coverage:-0}
          main_coverage=${main_coverage:-0}

          diff=$(echo "$current_coverage - $main_coverage" | bc 2>/dev/null || echo "0")

          echo "current=$current_coverage" >> $GITHUB_OUTPUT
          echo "main=$main_coverage" >> $GITHUB_OUTPUT
          echo "diff=$diff" >> $GITHUB_OUTPUT

          echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Current Coverage | ${current_coverage}% |" >> $GITHUB_STEP_SUMMARY
          echo "| Main Branch Coverage | ${main_coverage}% |" >> $GITHUB_STEP_SUMMARY
          echo "| Difference | ${diff}% |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if (( $(echo "$diff > 0" | bc -l 2>/dev/null || echo "0") )); then
            echo "✅ Coverage increased by ${diff}%" >> $GITHUB_STEP_SUMMARY
          elif (( $(echo "$diff < 0" | bc -l 2>/dev/null || echo "0") )); then
            echo "⚠️ Coverage decreased by ${diff#-}%" >> $GITHUB_STEP_SUMMARY
          else
            echo "➡️ Coverage unchanged" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Post coverage comment on PR
        if: hashFiles('coverage.out') != ''
        run: |
          current="${{ steps.coverage-report.outputs.current }}"
          main="${{ steps.main-coverage.outputs.main_coverage }}"
          diff="${{ steps.coverage-report.outputs.diff }}"

          current=${current:-0}
          main=${main:-0}
          diff=${diff:-0}

          if (( $(echo "$diff > 0" | bc -l 2>/dev/null || echo "0") )); then
            diff_formatted="+${diff}%"
            emoji="📈"
          elif (( $(echo "$diff < 0" | bc -l 2>/dev/null || echo "0") )); then
            diff_formatted="${diff}%"
            emoji="📉"
          else
            diff_formatted="unchanged"
            emoji="➡️"
          fi

          low_coverage=$(go tool cover -func coverage.out | grep -v "total:" | \
            awk -F'[\t:]' '{
              split($2,pct,"%")
              if (pct[1] < 50 && pct[1] > 0) {
                path = $1
                n = split(path, parts, "/")
                file = parts[n]
                if (!(file in min) || pct[1] < min[file]) {
                  min[file] = pct[1]
                  paths[file] = path
                }
              }
            } END {
              for (file in min) {
                printf "| %3d%% | %s |\n", min[file], paths[file]
              }
            }' | sort -t'|' -k2 -n | head -10)

          COMMENT="## Coverage Report ${emoji}

          | Metric | Value |
          |--------|-------|
          | **This PR** | ${current}% |
          | **Main Branch** | ${main}% |
          | **Change** | ${diff_formatted} ${emoji} |"

          if [ -n "$low_coverage" ]; then
            COMMENT="${COMMENT}

          ### Files Needing Coverage

          | Coverage | File |
          |----------|------|
          ${low_coverage}"
          fi

          PR_NUMBER=${{ github.event.pull_request.number }}
          COMMENT_ID=$(gh api repos/${{ github.repository }}/issues/${PR_NUMBER}/comments \
            --jq '.[] | select(.body | startswith("## Coverage Report")) | .id' 2>/dev/null | head -1)

          if [ -n "$COMMENT_ID" ]; then
            gh api repos/${{ github.repository }}/issues/comments/${COMMENT_ID} \
              --method PATCH \
              --field body="${COMMENT}"
          else
            gh pr comment ${PR_NUMBER} --body "${COMMENT}"
          fi

  lint:
    name: Run Linter
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Run linter
        run: mise run lint

  build:
    name: Build Binary
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Build binary
        run: mise run build

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: blueprint-binaries
          path: blueprint-*

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [test, lint, build]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Download blueprint binaries
        uses: actions/download-artifact@v8
        with:
          name: blueprint-binaries

      - name: Make binaries executable
        run: |
          chmod +x blueprint-*

      - name: Run integration tests
        run: |
          mise run test:integration

  security:
    name: Security Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install mise
        uses: jdx/mise-action@v2

      - name: Run security scan
        run: mise run security
